{[
    MainFile
    ([
        declare int x : 10;
        declare char a : "b";

        int fact(int n)
        {
            if(n < 2)
            {
                return 1;
            }
            n : (- n 1);
            return (* (fact(n)) (+ n 1));
        }
    ])

    int main()
    {
        declare int u : (+ x 10);
        declare int v : 15;

        declare int i;

        loop(i : 5; i > 0; i : (- i 1))
        {
            u : (/ (fact(u)) v);
            if(u < v)
            {
                break;
            }
            else
            {
                continue;
            }
        }

        return 0;
    }

]}

{[
    SubFile1
    ([
        declare int y : 20;

        int mul(int a1 , int a2)
        {
            return (* a1 a2);
        }
    ])

    int main()
    {
        declare char b : a;

        declare int A[5][5];
        declare int i:0;
        declare int j:0;
        loop(i: 0; i < 10; i:(+ i 1))
        {
            loop(j : 0; j < 10; j : (+ j 1))
            {
                A[i][j]: (mul(i, j));
            }
        }
        declare int B[5][5] : A;

        A : (+ A B);
        B : (* B A);
        A : (- (+ A B) (/ (* B A) 1));

        return 0;
    }

]}

{[
    SubFile2
    ([
        declare int z : 40;
    ])

    int main()
    {
        declare int m : (% x (* y (- z 7)));

        TeX
        {
            [: (mul(1, (mul(m, 4)))) :],
            "Hello!",
            [: (fact((mul(x, y)))) :],
            "Bye :)"
        }

        return 0;
    }

]}