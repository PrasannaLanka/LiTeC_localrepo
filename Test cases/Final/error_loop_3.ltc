{[
    MainFile
    ([
        declare int x : 10;
        declare char a : "b";
    ])

    int main()
    {
        declare int u : (+ x 10);
        
        declare int v : 15;
        
        declare int i;

        loop(i : 5; i > 0; i : (- i 1))
        {
            u : (/ u v);
        }

        loop(v : 25;true)           /* Parsing error since semi-colon is missing after the loop condition */
        {
            u : (* u v);
        }

        loop(;false;v : (+ v 1))
        {
            u : (% u v);
        }

        return 0;
    }

]}